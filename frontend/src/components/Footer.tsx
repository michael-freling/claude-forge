/** Footer is the muted page footer naming the backing RPC. */
export function Footer() {
  return (
    <footer className="foot">
      claude-forge dashboard · dashboard.v1.DashboardService/GetDashboard
    </footer>
  );
}
